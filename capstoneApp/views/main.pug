extends mainLayout.pug

block content 
    for recipeData in recipesArray
        .recipe-card
            h3 #{recipeData.name}
            h4 Ingredients:
            ul
                each ingredient in recipeData.ingredients
                    li= ingredient
            h4 Steps:
            ol
                each step in recipeData.steps
                    li= step
